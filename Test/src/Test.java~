public static class GeometricObject{
    
    public static displayGeometricObject(Geometric geometricObj){
        System.out.println("Created on " + geometricObj.getDateCreated()+
                ". Color is " +geometricObj.getColor());
    }
    public getGeometricObject(){
        return new Geometric();
    }
}