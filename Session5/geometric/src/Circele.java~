public class Circele extends Geometric {
    private double radius;

    public Circele(String color, boolean filled, double radius) {
        super(color, filled);
        this.radius = radius;
    }
}